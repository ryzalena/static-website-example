# Используем легковесный образ nginx для статического сайта
FROM nginx:alpine

# Устанавливаем рабочую директорию
WORKDIR /usr/share/nginx/html

# Удаляем стандартные файлы nginx
RUN rm -rf ./*

# Копируем файлы вашего статического сайта
COPY . .

# Открываем порт 80
EXPOSE 80

# Команда запуска уже содержится в базовом образе nginx
# nginx запускается автоматически
