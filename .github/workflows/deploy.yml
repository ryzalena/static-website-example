script: |
  echo "🚀 Начало деплоя приложения..."
  
  # 1. Определяем домашнюю директорию
  HOME_DIR="/home/$USER"
  APP_DIR="$HOME_DIR/app"
  
  echo "📁 Домашняя директория: $HOME_DIR"
  echo "📁 Директория приложения: $APP_DIR"
  
  # 2. Создаем директорию если не существует
  if [ ! -d "$APP_DIR" ]; then
    echo "📂 Создаем директорию приложения..."
    mkdir -p "$APP_DIR"
  fi
  
  cd "$APP_DIR"
  echo "📂 Текущая директория: $(pwd)"
  
  # 3. Останавливаем текущее приложение если запущено
  if [ -f app.pid ]; then
    echo "⏸️ Останавливаем текущее приложение..."
    kill $(cat app.pid) 2>/dev/null || true
    rm -f app.pid
  fi
  
  # 4. Копируем файлы с помощью scp (нужно добавить отдельный шаг)
  echo "📤 Файлы будут скопированы отдельным шагом..."
  
  # 5. Устанавливаем зависимости если нужно
  if [ -f requirements.txt ]; then
    echo "📦 Устанавливаем зависимости Python..."
    pip3 install -r requirements.txt --user
  else
    echo "ℹ️ requirements.txt не найден"
  fi
  
  # 6. Запускаем приложение (предполагаем что app.py уже есть)
  if [ -f app.py ]; then
    echo "🚀 Запускаем приложение..."
    nohup python3 app.py > app.log 2>&1 &
    echo $! > app.pid
    
    # 7. Проверяем что приложение запустилось
    sleep 3
    if ps -p $(cat app.pid) > /dev/null; then
      echo "✅ Приложение успешно запущено (PID: $(cat app.pid))"
      echo "📝 Логи приложения:"
      tail -5 app.log
    else
      echo "❌ Ошибка: приложение не запустилось"
      echo "🔍 Последние логи:"
      tail -20 app.log
      exit 1
    fi
  else
    echo "⚠️ app.py не найден в $APP_DIR"
    echo "Список файлов:"
    ls -la
  fi
  
  # 8. Проверяем доступность приложения
  echo "🔍 Проверяем доступность приложения..."
  APP_URL="http://app.ryzhova.course.prafdin.ru"
  echo "🌐 URL приложения: $APP_URL"
  
  if curl -s -f --max-time 10 "$APP_URL" > /dev/null; then
    echo "✅ Приложение доступно по адресу: $APP_URL"
    echo "📊 Информация о приложении:"
    curl -s "$APP_URL" | head -5
  else
    echo "⚠️ Предупреждение: не удалось подключиться к $APP_URL"
    echo "Проверьте что:"
    echo "1. Приложение запущено на порту 8181"
    echo "2. FRP настроен правильно"
    echo "3. Порт 8181 проброшен через FRP"
  fi
  
  echo "🎉 Деплой завершен!"
